package com.khaled.donation.Listeners;

import android.view.View;

import com.khaled.donation.Models.Notifications;

public interface OnClickNotificationListener {
    void OnClickListener(Notifications notifications, View view);
}
